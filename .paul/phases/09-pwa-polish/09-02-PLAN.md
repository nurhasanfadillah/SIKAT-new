---
phase: 09-pwa-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - vite.config.ts
  - index.html
  - .paul/codebase/INTEGRATIONS.md
autonomous: true
---

<objective>
## Goal
Polish PWA manifest metadata (categories) + consolidate favicon declarations + document PWA system in INTEGRATIONS.md.

## Purpose
Manifest categories improve store discoverability (TWA). Dual favicon declarations are redundant. INTEGRATIONS.md lacks PWA section — needed for maintainability.

## Output
- `vite.config.ts` — manifest `categories` field added
- `index.html` — favicon.ico reference removed, logo.svg remains as sole favicon
- `.paul/codebase/INTEGRATIONS.md` — new PWA section documenting full system
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@vite.config.ts
@index.html
@.paul/codebase/INTEGRATIONS.md
@.paul/phases/09-pwa-polish/09-01-SUMMARY.md
</context>

<acceptance_criteria>

## AC-1: Manifest categories
```gherkin
Given manifest.webmanifest is generated during vite build
When inspecting the manifest JSON
Then it contains "categories": ["finance", "education", "productivity"]
```

## AC-2: Single favicon source
```gherkin
Given index.html is loaded by browser
When inspecting <link rel="icon"> elements
Then only logo.svg is declared (no favicon.ico reference)
And the SVG favicon is the sole icon source
```

## AC-3: INTEGRATIONS.md PWA section
```gherkin
Given INTEGRATIONS.md is read by a developer
When searching for PWA documentation
Then a dedicated PWA section exists covering: plugin, build output, sw.js, manifest, icons, offline fallback, update prompt, install prompt
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest categories</name>
  <files>vite.config.ts</files>
  <action>
    In VitePWA manifest object (between scope and icons), add:
    ```
    categories: ['finance', 'education', 'productivity'],
    ```
    
    Avoid: changing any other manifest fields
    Avoid: adding screenshots array (requires actual screenshot images not yet available)
  </action>
  <verify>
    Build: `npm run build` → dist/manifest.webmanifest contains "categories":["finance","education","productivity"]
    grep: Select-String dist/manifest.webmanifest -Pattern "categories"
  </verify>
  <done>AC-1 satisfied: Manifest contains categories field</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate favicon — keep logo.svg only</name>
  <files>index.html</files>
  <action>
    Remove the line:
    `<link rel="icon" href="/favicon.ico" sizes="any">`
    
    Keep only:
    `<link rel="icon" href="/logo.svg" type="image/svg+xml">`
    
    SVG favicon is supported by all modern browsers and scales to any size.
    favicon.ico file stays in public/ (still served for legacy clients that look for /favicon.ico).
    
    Avoid: removing the SVG favicon line
    Avoid: touching any other meta/link tags
  </action>
  <verify>
    Read index.html → only one `<link rel="icon">` pointing to logo.svg
    Read index.html → no `favicon.ico` reference in link tags
  </verify>
  <done>AC-2 satisfied: Single favicon source (logo.svg)</done>
</task>

<task type="auto">
  <name>Task 3: Add PWA section to INTEGRATIONS.md</name>
  <files>.paul/codebase/INTEGRATIONS.md</files>
  <action>
    Add new section after "CI/CD & Deployment" and before "Environment Configuration":
    
    ```
    ## PWA (Progressive Web App)
    
    **Plugin:** vite-plugin-pwa v1.3.0 (generateSW strategy)
    
    **Service Worker:**
    - Auto-generated via Workbox at build time (`dist/sw.js`)
    - `registerType: 'autoUpdate'` — automatic updates without user prompt
    - Precache: 21 entries (all JS/CSS/HTML/icons)
    - Runtime cache: `NetworkFirst` for `/api/*` (10s timeout, 50 entries, 300s TTL)
    
    **Offline Support:**
    - `navigateFallback: '/offline.html'` — branded fallback page when network unavailable
    - Allowlist excludes `/api/*` from navigation interception
    
    **Manifest:**
    - `manifest.webmanifest` generated at build, injected into index.html
    - `display: standalone` — native app experience
    - Theme/bg color: `#2563EB` (brand blue)
    - Categories: finance, education, productivity
    
    **Icons:**
    - Generated from `public/logo.svg` via `@vite-pwa/assets-generator` (`minimal2023Preset`)
    - Sizes: 64x64, 192x192, 512x512 (standard) + 512x512 (maskable)
    - Apple Touch: 180x180
    
    **UX Components:**
    - **SplashScreen** (`src/components/SplashScreen.tsx`) — Framer Motion, 2.5s display, renders before AuthProvider
    - **ReloadPrompt** (`src/components/ReloadPrompt.tsx`) — toast "Versi baru tersedia" via `useRegisterSW`
    - **InstallPrompt** (`src/components/InstallPrompt.tsx`) — floating install button via `beforeinstallprompt`
    
    **Build Script:** `npm run pwa:assets` regenerates all PWA icon sizes from source SVG.
    ```
    
    Avoid: removing any existing sections
    Avoid: duplicating information already in STACK.md (keep PWA section there too — STACK = what, INTEGRATIONS = how)
  </action>
  <verify>
    Read .paul/codebase/INTEGRATIONS.md → contains `## PWA` section with all subsections
    Read .paul/codebase/INTEGRATIONS.md → existing sections (APIs, Data Storage, Auth, CI/CD, Env) preserved
  </verify>
  <done>AC-3 satisfied: INTEGRATIONS.md has comprehensive PWA documentation</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- public/favicon.ico — keep file on disk for legacy clients accessing `/favicon.ico` directly
- vite.config.ts beyond manifest object (no plugin, workbox, resolve, or server changes)
- src/components/* — no component changes
- .paul/ROADMAP.md, .paul/STATE.md — updated by plan/unify workflow, not by tasks

## SCOPE LIMITS
- Screenshots in manifest deferred — requires actual screenshot images (future task)
- No PWA functional changes — this plan is manifest metadata + docs only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` completes
- [ ] dist/manifest.webmanifest contains `categories` array
- [ ] index.html has single favicon (logo.svg)
- [ ] INTEGRATIONS.md has PWA section between CI/CD and Environment Configuration
</verification>

<success_criteria>
- All 3 tasks completed with AC-1/AC-2/AC-3 satisfied
- Build output unchanged except for manifest categories addition
- No regressions in existing behavior
</success_criteria>

<output>
After completion, create `.paul/phases/09-pwa-polish/09-02-SUMMARY.md`
</output>

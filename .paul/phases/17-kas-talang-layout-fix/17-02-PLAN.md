---
phase: 17-kas-talang-layout-fix
plan: 02
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - src/pages/Kas.tsx
autonomous: false
---

<objective>
## Goal
Redesign card transaksi di halaman Kas Rekap: tanggal menjadi primary visual di kolom kiri (rowspan-2), detail disembunyikan di balik expanded area yang toggle saat card diklik.

## Purpose
Card saat ini menampilkan icon jenis (TrendingUp/Down), sumber/kategori, nominal, dan tombol edit/delete sekaligus — terlalu padat. Layout baru: hanya tampilkan tanggal + keterangan + nominal. Detail (jenis, sumber/kategori, tanggal penuh, edit/delete) muncul inline saat card diklik. UX lebih bersih, fokus ke informasi terpenting.

## Output
- `Kas.tsx` — card transaction list dengan layout baru: tanggal-first 2-row grid + expandedId state + animated expand area
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/17-kas-talang-layout-fix/DISCOVERY.md
@.paul/phases/17-kas-talang-layout-fix/17-01-SUMMARY.md
@src/pages/Kas.tsx
</context>

<acceptance_criteria>

## AC-1: Card menampilkan tanggal sebagai primary element di kolom kiri
```gherkin
Given halaman Kas Rekap dengan transaksi
When card ditampilkan dalam list
Then kolom 1 (rowspan-2) menampilkan tanggal dalam format "dd" (angka) dan "MMM" (bulan singkat)
And kolom kanan atas menampilkan keterangan (truncate)
And kolom kanan bawah menampilkan nominal dengan warna brand-500 (pemasukan) atau rose-400 (pengeluaran)
And icon TrendingUp/Down TIDAK tampil di card summary (dipindah ke expanded area)
```

## AC-2: Card expand saat diklik, accordion behavior
```gherkin
Given card transaksi di Kas Rekap
When user tap/click card
Then expanded area muncul dengan animasi (height 0 → auto)
And expanded area menampilkan: badge jenis (icon + teks), sumber/kategori, tanggal lengkap (EEEE dd MMMM yyyy)
And tombol edit/delete muncul di expanded area (hanya jika isBendahara dan bukan Pelunasan Dana Talang)
And kolom 3 row 2 menampilkan ChevronDown yang rotasi 180° saat expanded
When user tap card yang sama lagi
Then expanded area menutup dengan animasi
```

## AC-3: Expand dan edit tidak saling konflik
```gherkin
Given card yang sedang expanded (expandedId = t.id)
When user klik tombol Edit di expanded area
Then form edit terbuka (editingId set) dan expandedId di-reset ke null
And card mendapat border-brand-500 highlight (editingId styling tetap berjalan)

Given card yang sedang di-edit (editingId = t.id)
When card tersebut diklik
Then expand TIDAK toggle (karena editingId === t.id = guard aktif)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Tambah imports baru dan expandedId state ke Kas.tsx</name>
  <files>src/pages/Kas.tsx</files>
  <action>
    1. **Tambah `ChevronDown` ke lucide import** (line 12):
       Sebelum: `import { TrendingUp, TrendingDown, Plus, X, Calendar, Wallet, Search, SlidersHorizontal, RotateCcw, Edit2, Trash2 } from 'lucide-react';`
       Sesudah: `import { TrendingUp, TrendingDown, Plus, X, Calendar, Wallet, Search, SlidersHorizontal, RotateCcw, Edit2, Trash2, ChevronDown } from 'lucide-react';`

    2. **Tambah motion/react import** setelah baris import terakhir (setelah line 13):
       `import { motion, AnimatePresence } from 'motion/react';`

    3. **Tambah `expandedId` state** di bawah `editingId` (sekitar line 29):
       `const [expandedId, setExpandedId] = useState<string | null>(null);`

    4. **Reset expandedId di `handleStartEdit`**: Cari fungsi `handleStartEdit` (sekitar line 30), tambahkan `setExpandedId(null);` di awal body fungsi, sebelum baris `setEditingId(t.id)` atau sejenisnya.

    Jangan ubah: fungsi lain, form state, filter logic, handleSubmit, handleDelete, handleCancelEdit.
  </action>
  <verify>
    `tsc --noEmit` di root project — zero errors.
    Cek bahwa `expandedId` state ada dan `ChevronDown`, `motion`, `AnimatePresence` ter-import.
  </verify>
  <done>Imports dan state siap — AC-2 prerequisite terpenuhi (expandedId tersedia)</done>
</task>

<task type="auto">
  <name>Task 2: Redesign card JSX — tanggal-first 2-row grid + expand area</name>
  <files>src/pages/Kas.tsx</files>
  <action>
    Ganti seluruh konten card mapping (dari pembuka `<div key={t.id} className={...grid...}>` hingga penutup `</div>` sebelum empty state div) dengan layout baru berikut:

    ```jsx
    <div
      key={t.id}
      className={`p-3 rounded-2xl bg-surface-card/60 border transition-all grid grid-cols-[auto_1fr_auto] gap-x-3 gap-y-1 cursor-pointer ${
        editingId === t.id
          ? 'border-brand-500 bg-surface-card/95 shadow-[0_0_15px_rgba(0,229,163,0.15)]'
          : 'border-white/5 hover:border-white/10'
      }`}
      onClick={() => {
        if (editingId === t.id) return;
        setExpandedId(expandedId === t.id ? null : t.id);
      }}
    >
      {/* Kolom 1 rowspan-2: Tanggal mini card */}
      <div className="col-start-1 col-end-2 row-start-1 row-end-3 flex flex-col items-center justify-center self-center w-10 min-h-[2.75rem] bg-white/5 rounded-xl border border-white/10 px-1 gap-0.5">
        <span className="text-nano text-slate-500 font-medium leading-none uppercase">
          {formatIgnoreTimezone(t.tanggal, 'MMM', { locale: localeId })}
        </span>
        <span className="text-value font-black text-slate-100 leading-none">
          {formatIgnoreTimezone(t.tanggal, 'dd', { locale: localeId })}
        </span>
      </div>

      {/* Kolom 2+3 row 1: Keterangan */}
      <div className="col-start-2 col-end-4 row-start-1 row-end-2 min-w-0 flex items-end pb-0.5">
        <span className="text-body font-bold text-slate-100 block truncate leading-tight">
          {t.keterangan}
        </span>
      </div>

      {/* Kolom 2 row 2: Nominal */}
      <div className="col-start-2 col-end-3 row-start-2 row-end-3 flex items-center">
        <span className={`text-body font-black ${isPemasukan ? 'text-brand-500' : 'text-rose-400'}`}>
          {isPemasukan ? '+' : '-'}{formatCurrency(t.nominal)}
        </span>
      </div>

      {/* Kolom 3 row 2: Chevron toggle */}
      <div className="col-start-3 col-end-4 row-start-2 row-end-3 flex items-center justify-end">
        <motion.div
          animate={{ rotate: expandedId === t.id ? 180 : 0 }}
          transition={{ duration: 0.2 }}
        >
          <ChevronDown className="h-4 w-4 text-slate-500" />
        </motion.div>
      </div>

      {/* Expanded detail area — full width */}
      <AnimatePresence>
        {expandedId === t.id && (
          <motion.div
            initial={{ opacity: 0, height: 0 }}
            animate={{ opacity: 1, height: 'auto' }}
            exit={{ opacity: 0, height: 0 }}
            transition={{ duration: 0.2 }}
            className="col-start-1 col-end-4 overflow-hidden"
            onClick={e => e.stopPropagation()}
          >
            <div className="border-t border-white/5 mt-1 pt-2 space-y-2">
              {/* Badge jenis + sumber/kategori */}
              <div className="flex flex-wrap gap-1.5">
                <span className={`flex items-center gap-1 px-1.5 py-0.5 rounded text-nano border ${
                  isPemasukan
                    ? 'bg-brand-500/10 text-brand-500 border-brand-500/10'
                    : 'bg-rose-500/10 text-rose-400 border-rose-500/10'
                }`}>
                  {isPemasukan ? <TrendingUp className="h-3 w-3" aria-hidden="true" /> : <TrendingDown className="h-3 w-3" aria-hidden="true" />}
                  {t.jenis}
                </span>
                <span className="bg-white/5 px-1.5 py-0.5 rounded text-nano text-slate-300 border border-white/5">
                  {isPemasukan ? t.sumber_dana : t.kategori}
                </span>
              </div>
              {/* Tanggal lengkap */}
              <div className="text-nano text-slate-500">
                {formatIgnoreTimezone(t.tanggal, 'EEEE, dd MMMM yyyy', { locale: localeId })}
              </div>
              {/* Action buttons */}
              {isBendahara && (
                t.kategori === 'Pelunasan Dana Talang' ? (
                  <div className="text-micro text-slate-500 italic text-right">
                    Kelola di Dana Talang
                  </div>
                ) : (
                  <div className="flex gap-1.5 justify-end">
                    <button
                      onClick={() => handleStartEdit(t)}
                      className="flex items-center gap-1 px-2 py-1 rounded-lg text-nano text-slate-400 hover:text-brand-500 hover:bg-white/5 border border-white/5 transition-colors"
                    >
                      <Edit2 className="h-3 w-3" />
                      Ubah
                    </button>
                    <button
                      onClick={async () => {
                        const isConfirmed = await confirm({
                          title: 'Hapus Transaksi Kas',
                          message: `Apakah Anda benar-benar yakin ingin melenyapkan catatan "${t.keterangan}" senilai ${formatCurrency(t.nominal)} secara permanen?`,
                          confirmLabel: 'Ya, Hapus',
                          cancelLabel: 'Batal',
                          variant: 'danger'
                        });
                        if (isConfirmed && t.id) {
                          await handleDelete(t.id);
                        }
                      }}
                      className="flex items-center gap-1 px-2 py-1 rounded-lg text-nano text-slate-400 hover:text-rose-400 hover:bg-white/5 border border-white/5 transition-colors"
                    >
                      <Trash2 className="h-3 w-3" />
                      Hapus
                    </button>
                  </div>
                )
              )}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
    ```

    Pastikan: `const isPemasukan = t.jenis === 'Pemasukan';` masih di dalam `.map()` sebelum return.
    Jangan ubah: empty state div di bawah list, Card wrapper, filter section, form section.
    Jangan ubah: `formatCurrency` usage — fungsi ini sudah ada di scope.
  </action>
  <verify>
    `tsc --noEmit` di root project — zero errors.
    Re-read card mapping area di Kas.tsx untuk konfirmasi struktur baru terpasang.
  </verify>
  <done>AC-1, AC-2, AC-3 terpenuhi secara struktural — layout dan state sudah benar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Card transaksi Kas Rekap didesain ulang:
    - Kolom kiri (rowspan-2): mini card tanggal (angka + bulan singkat)
    - Baris 1 kanan: Keterangan (truncate)
    - Baris 2 kanan: Nominal (warna brand/rose) + ChevronDown
    - Klik card → expand area (jenis badge, sumber/kategori, tanggal penuh, tombol Ubah/Hapus)
    - Chevron rotasi 180° saat expanded
    - Klik card lagi → collapse
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev` (jika belum)
    2. Buka: http://localhost:5173/kas (atau port yang aktif)
    3. Cek card summary: tampil tanggal di kiri, keterangan di kanan atas, nominal di kanan bawah
    4. Cek TIDAK ada icon TrendingUp/Down di card summary (hanya di expanded)
    5. Klik card → expanded area muncul smooth dengan badge jenis, sumber/kategori, tanggal penuh
    6. Cek akun Bendahara: tombol Ubah + Hapus muncul di expanded area
    7. Klik card lagi → collapse smooth
    8. Klik Ubah → form edit terbuka, expanded collapse (expandedId reset)
    9. Hapus satu transaksi via expanded area → berjalan normal
    10. Scroll ke bawah — last item tetap visible di atas nav bar (baseline dari 17-01)
  </how-to-verify>
  <resume-signal>Type "approved" atau deskripsikan issue yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Form edit logic (handleStartEdit, handleSubmit, handleCancelEdit, handleDelete) — hanya tambahkan `setExpandedId(null)` di handleStartEdit
- Filter section dan filter logic
- Card component wrapper (`<Card>`)
- `formatCurrency`, `formatIgnoreTimezone` — dipakai as-is
- Layout.tsx, Talang.tsx — tidak disentuh plan ini
- scrollIntoView di handleStartEdit — tetap valid

## SCOPE LIMITS
- Plan ini hanya Kas.tsx (Talang.tsx adalah scope 17-03)
- Tidak ada perubahan API atau data fetching
- Tidak ada package install baru (motion/react sudah ada di codebase via Layout.tsx)
- Card untuk transaksi `Pelunasan Dana Talang` tetap menampilkan teks "Kelola di Dana Talang" (di expanded area, bukan di card summary)

</boundaries>

<verification>
Sebelum menyatakan plan selesai:
- [ ] `tsc --noEmit` di root project — zero errors
- [ ] `ChevronDown`, `motion`, `AnimatePresence` ter-import di Kas.tsx
- [ ] `expandedId` state ada di komponen
- [ ] `setExpandedId(null)` ada di `handleStartEdit`
- [ ] Card grid: col 1 rowspan-2 = tanggal, col 2+3 row 1 = keterangan, col 2 row 2 = nominal, col 3 row 2 = chevron
- [ ] AnimatePresence expand area: jenis badge + sumber/kategori + tanggal full + edit/delete
- [ ] Checkpoint human-verify approved
</verification>

<success_criteria>
- Semua 2 auto tasks selesai
- Checkpoint human-verify approved
- `tsc --noEmit` clean
- Card redesign visual sesuai dengan DISCOVERY.md target layout
- Expand/collapse berfungsi dengan animasi smooth
- Edit dan Delete dari expanded area berfungsi normal
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/17-kas-talang-layout-fix/17-02-SUMMARY.md`
</output>

---
phase: 16-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/Laporan.tsx
  - src/App.tsx
  - src/components/Layout.tsx
  - package.json
autonomous: true
---

<objective>
## Goal
Hapus section "Rincian Per Akun Talang" dari Dashboard dan hapus seluruh halaman Laporan beserta route, navigasi, dan dependency recharts-nya.

## Purpose
Informasi per-akun talang sudah tersedia di halaman Talang. Halaman Laporan redundan karena data keuangan sudah terwakili di Dashboard, Kas Rekap, dan Dana Talang. Menghapus keduanya menyederhanakan UI dan mengurangi bundle size.

## Output
- Dashboard lebih ringkas: hero card + mini stats + recent transactions
- Navigasi bottom bar: 3 item (Beranda · Kas Rekap · Dana Talang)
- Laporan.tsx dihapus, recharts di-uninstall
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@src/pages/Dashboard.tsx
@src/pages/Laporan.tsx
@src/App.tsx
@src/components/Layout.tsx
</context>

<acceptance_criteria>

## AC-1: Rincian Per Akun Talang dihapus dari Dashboard
```gherkin
Given halaman Dashboard terbuka
When user melihat halaman dashboard
Then tidak ada card "Rincian Per Akun Talang" atau daftar per-akun (Jisoi/Rakka/Shae)
And section hero card, mini stats (Dana Talang Aktif + Kapasitas Sisa), dan Riwayat Transaksi Terkini tetap tampil normal
```

## AC-2: Halaman Laporan tidak bisa diakses
```gherkin
Given user sedang login
When user navigasi ke /laporan (manual di URL bar)
Then app redirect ke dashboard atau render 404
And tidak ada link/nav item "Laporan" di bottom navigation
```

## AC-3: recharts tidak ada di dependencies
```gherkin
Given project setelah perubahan
When npm ls recharts dijalankan
Then recharts tidak ditemukan di node_modules
And TypeScript build (tsc --noEmit) berhasil tanpa error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Hapus card "Rincian Per Akun Talang" dari Dashboard.tsx</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Di Dashboard.tsx, hapus block berikut secara tepat:

    1. Hapus wrapper div "Bento Grid" dan Card "Rincian Per Akun Talang":
       - Hapus comment `{/* Bento Grid: Accounts list & recent transaction summary */}`
       - Hapus opening tag `<div className="space-y-4">` yang membungkus Rincian + Recent Transactions
       - Hapus seluruh `<Card>` dengan judul "Rincian Per Akun Talang" (dari opening Card tag hingga closing `</Card>`)
       - Hapus closing `</div>` yang menutup wrapper tadi
       - Biarkan section `{/* Recent Transactions List with colored pill icons */}` tetap ada sebagai direct child dari outer `<div className="space-y-4">` di return

    2. Verifikasi `talangBalances` dan kalkulasinya (baris ~31-43) TETAP ada — masih dipakai untuk `totalTalangAktif`, `saldoBersih`, `talangPct`, `saldoPct` di bagian mini cards.

    3. Import `ListCollapse` dari lucide-react TETAP ada — masih dipakai di Recent Transactions section.

    Avoid: jangan hapus kalkulasi talangBalances, jangan hapus mini cards grid (Dana Talang Aktif + Kapasitas Sisa).
  </action>
  <verify>Buka src/pages/Dashboard.tsx dan pastikan tidak ada `Rincian Per Akun Talang`, tidak ada `Object.entries(talangBalances).map`, tapi `talangBalances` dan `totalTalangAktif` masih ada.</verify>
  <done>AC-1 satisfied: card Rincian Per Akun Talang dihapus, kalkulasi dan mini cards tetap utuh</done>
</task>

<task type="auto">
  <name>Task 2: Hapus Laporan.tsx dan bersihkan App.tsx</name>
  <files>src/pages/Laporan.tsx, src/App.tsx</files>
  <action>
    1. Hapus file `src/pages/Laporan.tsx` sepenuhnya.

    2. Di `src/App.tsx`:
       - Hapus baris: `import Laporan from './pages/Laporan';`
       - Hapus baris: `<Route path="laporan" element={<Laporan />} />`
       - Biarkan semua route lain (/, /kas, /talang) tetap intact.

    Avoid: jangan ubah ProtectedRoute, jangan ubah struktur Router/Routes.
  </action>
  <verify>File src/pages/Laporan.tsx tidak ada. Di App.tsx tidak ada string "Laporan" (import maupun Route).</verify>
  <done>AC-2 satisfied (partial): route /laporan dihapus dari app</done>
</task>

<task type="auto">
  <name>Task 3: Hapus navigasi Laporan dari Layout.tsx dan uninstall recharts</name>
  <files>src/components/Layout.tsx, package.json</files>
  <action>
    1. Di `src/components/Layout.tsx`:
       - Hapus `BarChart2` dari import lucide-react (baris import lucide)
       - Hapus object `{ name: 'Laporan', href: '/laporan', icon: BarChart2 }` dari array `navigation`
       - Hapus `case '/laporan': return 'Sensus Keuangan';` dari fungsi `getPageTitle()`
       - Navigation array tersisa 3 item: Beranda, Kas Rekap, Dana Talang

    2. Uninstall recharts:
       ```
       npm uninstall recharts
       ```
       (package.json dan package-lock.json diupdate otomatis)

    Avoid: jangan ubah styling nav bar, jangan hapus motion/AnimatePresence dari Layout.
  </action>
  <verify>
    1. Layout.tsx: tidak ada "Laporan", tidak ada "BarChart2", tidak ada "/laporan", navigation.length === 3
    2. npm ls recharts → "empty" atau tidak ditemukan
  </verify>
  <done>AC-2 satisfied (complete): nav item Laporan hilang. AC-3 satisfied: recharts tidak ada di dependencies.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/pages/Talang.tsx — tidak dalam scope
- src/pages/Kas.tsx — tidak dalam scope
- src/lib/tokens.ts — tokens tetap dipakai Dashboard dan halaman lain
- src/components/ui/tabs.tsx — biarkan saja meski unused; tidak dalam scope cleanup ini
- server.ts / api-handler.ts — backend tidak tersentuh
- vite.config.ts, vercel.json — konfigurasi tidak berubah

## SCOPE LIMITS
- Hanya hapus, jangan refactor atau tambah fitur baru
- Jangan migrasikan FeedbackContext.tsx arbitrary sizes (deferred dari Phase 07)
- Jangan hapus komponen UI lain yang mungkin unused
- Jangan ubah struktur kalkulasi talangBalances di Dashboard (masih dipakai mini cards)

</boundaries>

<verification>
Sebelum menyatakan plan selesai:
- [ ] `src/pages/Laporan.tsx` tidak ada (file dihapus)
- [ ] `src/App.tsx` tidak mengandung "Laporan" (import dan route)
- [ ] `src/components/Layout.tsx` navigation array memiliki 3 item (tanpa Laporan)
- [ ] `src/pages/Dashboard.tsx` tidak mengandung "Rincian Per Akun Talang" dan tidak ada `Object.entries(talangBalances).map`
- [ ] `talangBalances`, `totalTalangAktif`, `saldoBersih`, `talangPct`, `saldoPct` masih ada di Dashboard.tsx
- [ ] `npm ls recharts` mengembalikan empty / not found
- [ ] TypeScript build tidak error (jalankan `npx tsc --noEmit` atau `npm run build`)
</verification>

<success_criteria>
- Semua 3 task selesai
- Dashboard tidak menampilkan card Rincian Per Akun Talang
- Bottom nav menampilkan 3 item (Beranda · Kas Rekap · Dana Talang)
- Route /laporan tidak bisa diakses
- recharts tidak ada di package.json
- Build berhasil tanpa error TypeScript
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/16-ui-cleanup/16-01-SUMMARY.md`
</output>

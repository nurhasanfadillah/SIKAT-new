---
phase: 15-header-avatar-remove
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Hapus avatar circle dengan inisial nama user dari header Layout.tsx.
</objective>

<context>
@.paul/PROJECT.md
@src/components/Layout.tsx
</context>

<acceptance_criteria>
## AC-1: Avatar inisial tidak tampil di header
Given header dirender dengan user yang sudah login
When halaman apapun dibuka
Then avatar circle (h-10 w-10 rounded-full gradient) tidak tampil di header
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Hapus div avatar inisial dari header</name>
  <files>src/components/Layout.tsx</files>
  <action>
    Hapus blok berikut dari dalam left group header (sekitar line 53-56):
    ```
    {/* Quick Avatar Initials */}
    <div className="h-10 w-10 rounded-full bg-gradient-to-tr from-grd-start to-grd-end flex items-center justify-center font-bold text-surface-panel text-sm shadow-[0_0_15px_rgba(0,245,160,0.3)]">
      {profile?.nama?.[0]?.toUpperCase() || 'U'}
    </div>
    ```
    Hapus seluruh div (termasuk komentar) beserta isinya.
    Jangan ubah elemen lain: logo, text column, right group.
  </action>
  <verify>
    grep -n "rounded-full bg-gradient-to-tr" src/components/Layout.tsx
    Expected: no output
  </verify>
  <done>AC-1 satisfied: div avatar h-10 w-10 tidak ada di Layout.tsx</done>
</task>
</tasks>

<output>
After completion, create compressed .paul/phases/15-header-avatar-remove/15-01-SUMMARY.md
</output>
